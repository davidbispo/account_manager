module Services
  class CreateAccountService
    # def perform(db_instance, account_id, first_deposit=0)
    #   accounts = db_instance.from(:accounts)
    #   created_id = accounts.insert(balance: first_deposit)
    #   accounts.where(:id => created_id).first
    # end
  end
end